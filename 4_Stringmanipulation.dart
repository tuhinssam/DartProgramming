void main() {
  
  String str = "\tMy Name is\n Tuhin\n";
  
  //length
  print(str.length);
  
  // converts to uppercase
  print(str.toUpperCase());
  
  // converts to lowercase
  print(str.toLowerCase());
  
  //get the first occurence of patterns
  str.indexOf('me');
  
  //removed trailing whitespaces
  print(str.trim());
  
  
}
